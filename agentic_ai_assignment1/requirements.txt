# Core / shared
python-dotenv>=1.0.0
requests>=2.31.0

# Task 4: RAG Q&A
scikit-learn>=1.3.0
pypdf>=3.15.0

# Optional LLM backends -- install whichever you plan to use.
# Ollama needs no Python package (it's a local server); just install
# https://ollama.com and run `ollama pull llama3` before running the scripts.
openai>=1.30.0
google-generativeai>=0.5.0
